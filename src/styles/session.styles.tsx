import styled from "@emotion/styled";

import Grid from '@mui/material/Unstable_Grid2'; // Grid version 2

export const SessionPageContainer = styled(Grid)(() => ({
    flexDirection: 'column',
    margin: '1%',
}))